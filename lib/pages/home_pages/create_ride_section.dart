import 'package:flutter/material.dart';

class CreateRideSection extends StatelessWidget {
  const CreateRideSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Create Ride UI here ➕",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
